package org.logic2j.predsolver.api;


/**
 * A {@link Provider} "owns" the definition of {@link Predicate}s.
 * FIXME Provider is actually more a factory than a "provider". And do we need it really? 
 * If logic is done in the Solver, not here, we may not need this hierarchy of classes at all.
 * 
 * @author Laurent
 */
public interface Provider {

}
